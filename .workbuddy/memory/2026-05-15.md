# 2026-05-15 工作日志

## P2 遗传进化入库门槛修复
- 文件：`strategy/genetic_evolver.py` 第 890 行附近
- 问题：原门槛 `fitness <= 0.5` 过高（需顶级基金级别才能入库），导致 P2 始终 0 条规则入库
- 修复（三重过滤）：
  1. `fitness <= 0.15`（原 0.5）
  2. `win_rate < 50` 强制最低胜率
  3. `total_trades < 5` 排除噪声规则

## P3 LGBM 训练打标修复
- 文件：`services/strategy_lab_service.py`
- 问题：`get_signals_for_training()` 查询 `label_return IS NOT NULL`，但该字段从未被写入
- 修复：新增 `_backfill_signal_labels()` 函数，在 `run_lgbm_train()` 前自动回填
  - 用信号日+5天后的收盘价计算5日超额收益（扣除同期指数收益）
  - 自动填充 `label_return` 和 `is_win` 字段

## P4 动态选择器约束放宽
- 文件：`strategy/dynamic_selector.py`
- 问题1：`apply_constraints()` 中 `per_stock_trades < 3`（total_trades/sample_count）在60/120天切片下几乎全过滤
- 修复1：改为总量门槛 `total_trades < 3`
- 问题2：`get_turnover_penalty()` 中 `est_turnover > 2.0` 过于激进
- 修复2：阈值从 2.0/1.5 放宽到 5.0/3.0
